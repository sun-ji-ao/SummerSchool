export default function CartPage() {
  return (
    <main className="mx-auto w-full max-w-4xl px-6 py-10">
      <h1 className="text-3xl font-semibold">Cart & Deposit</h1>
      <p className="mt-2 text-slate-600">用于后续接入 Stripe Checkout 定金支付。</p>
    </main>
  );
}
